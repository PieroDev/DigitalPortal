import { FadeInViewDirective } from './fade-in-view.directive';

describe('FadeInViewDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInViewDirective();
    expect(directive).toBeTruthy();
  });
});
